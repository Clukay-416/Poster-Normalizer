name: Legacy publisher disabled
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Releases are built and verified by v04-validation.yml; existing releases are immutable.'
